# V1 原始文档归档

本目录完整保存 V2.0 重构前的 Markdown 原文和原目录层级，用于事实追溯、重建历史过程和核对迁移是否遗漏。

## 使用边界

- 当前任务默认不读取或修改这里的文件；
- 当前有效入口位于仓库根目录及 `00_` 至 `07_` 目录；
- 归档中的旧路径、流程和结论可能已被后续用户决定覆盖；
- 需要追溯具体日期、技术参数、旧版判断或完整机器转写时，再读取对应原文；
- 归档保留不等于其内容仍是当前规则。

`迁移映射.md` 记录旧文件在 V2 中的主要去向。没有被逐字复制到新权威文件的过程细节仍保留在本目录，因此原始内容没有删除。
